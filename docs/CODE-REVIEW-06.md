# Diff 阅读检查点

日期：2026-09-13；基线 `be9b970`。对上下文扩展、行内/空白呈现、搜索/锚点和偏好保存执行两轴独立只读评审；反例在隔离源码、target 与组件环境运行。

## Standards

1. 刷新后旧/新行号可分别命中不同的相同代码行，曾静默选第一个。现只接受唯一文本位置，歧义由 UI 解释并回到开头；原反例通过。
2. 初始化 preferences 与 recent_workspaces 的 Promise.all 曾使迟到结果覆盖已保存设置。现独立读取、单次启动、等待真实初始值后串行合并 partial；StrictMode 下 recent 晚到、prefs 晚到、连续操作、首次保存失败后后继成功均复测通过，已有字号和 Git 路径保留。
3. 密集搜索曾对 500 KB 文本产生 50 万片段，分段实测 2.014 秒。顺序扫描和命中/装饰限额后，相同反例为 50 个片段、0.72 毫秒；SSR 约 7.31 毫秒，浏览器为 100 个 span（含行容器共 150 个元素）。这是定向反例，不是发布 NFR。

以上已独立关闭，无额外高价值判断项。普通与密集搜索/空白/语法降级后的 DOM、Selection 保持原文，含 Tab、中文、CR。

增量复核又发现字号缩小仍沿用旧列宽，以及隐藏视图的零尺寸测量丢失阅读偏移。现按布局变化重置列宽、钳制横向偏移，不可见时冻结测量；垂直行高只缓存正值，同布局恢复不清空有效测量，保留换行行内偏移。独立组件验证滚动 1400→1400（行内 208px）、5500→5500（行内 18px）；12,050 行时缓存不超过 10,000 项，换快照后缓存重建，ResizeObserver 停止在稳定值，没有循环错误。全部追加发现已关闭。

## Spec

1. raw 曾显示结构化搜索计数/跳转入口，现切换时关闭并禁用该入口，模式说明明确。
2. raw 刷新曾提前推进上一快照标记，返回 Diff 时跳过歧义检查。锚点现自带快照 ID；独立组件确认唯一文本移位能跟随，重复文本明确回到开头。
3. 长行说明曾遗漏扩展上下文，现检查全部展示行；独立验证说明出现且原文、Patch、Review JSON 不变。

后端 5 条主回归及 2 条补充场景通过；额外覆盖中文/换行/前导横线未跟踪路径、无 EOF 换行，以及模式变化与两个文本单元共存时局部暂存不夹带执行位。

增量复核确认字号 24→11 后列宽与偏移按实际内容收缩，隐藏/返回时横向偏移不丢失。最后独立复测换行长行：并排行高 806px 时 scrollTop 491、行内偏移 443px 保持，统一行高 364px 时 scrollTop 456、行内偏移 200px 保持；源文、Patch、Review JSON 不变。全部定向发现已关闭，没有未处理的本轮阻断项。这些独立临时组件回归不等同于已纳入仓库的持续集成测试。

## 主工作区证据与限制

core 93、桌面 dispatch 1、配置规划 11、前端 13 条通过，Clippy/类型/格式/前端构建通过。完整 Rust 回归中的传输为 7/8：既有 `bridge_bounds_never_closed_stdin_and_missing_service` 再次超过 500 ms，总计 515.021292 ms、spawn 635.625 µs。直接重跑同一测试二进制通过；阈值未放宽、原因未关闭，本轮不能宣称完整回归通过。

浏览器核对了展开后仍为原两块、搜索 1/5→2/5、raw 搜索关闭、隐藏后的 +2/−2 真实计数与 Review 禁用、统一/并排顶部源行一致。1024×720 无全页横向溢出；右栏横向滚到最大 186px，左栏保持 0，长行末尾可达。

原生独立验证应用使用相同编译代码、独立 bundle ID/数据目录和真实隔离仓库。展开 25 行并隐藏空白后，index/工作树/Review 记录未变；随后显式审查并暂存第一块，磁盘确认只包含该块，另一空白变化仍未暂存，SQLite 有对应 `stage_migration`。另核对了真实 15 提交图及合并提交两个父提交的文件内容。具体证据和构建边界见 `ACCEPTANCE.md`；完整 P0、发布 NFR 与既有传输计时问题仍未完成。
