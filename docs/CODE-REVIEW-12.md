# 诊断导出与启动故障入口评审

基线：`52aaf2afe20a777b7bfa25cf2521e436d0f80c97`。范围：SET-03 正常/故障诊断、本地删除与预览生命周期；同时包含上一轮独立 Diff tab 的窄窗口可关闭性修复。

## Standards

独立 reviewer 使用隔离反例确认并关闭：旧预览在删除后重新绑定新代次；为记指标而重新等待 Git 核心锁；慢保存期间把已消费预览误报过期；应用信息入口通过目录别名写入产品数据目录。固定报告正文、现有文件保护、Core 失败下的 UI 入口均复验通过。最终定向复核硬问题 0，判断项 0。

## Spec

独立 reviewer 确认并关闭：删除之后旧内容仍可导出；安装状态白名单漏掉实际状态；保存成功同时残留过期提示；SQLite READWRITE 回退只读后失去删除顺序保护。补充独立 application_only 故障报告，明确缺失项，验证普通记录和修改后的返回正文不能通过该入口导出。最终定向复核未发现新增阻断项。

这份报告是本轮增量评审，不代表整份 PRD 已全部完成。原生调试窗口证据及保护边界见 `DIAGNOSTICS.md`。
