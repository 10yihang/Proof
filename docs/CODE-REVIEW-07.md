# 仓库布局检查点

2026-09-13，固定基线 `7dee898`，比较完整工作树 `git diff 7dee898 --`。Standards 与 Spec 在独立源码/组件夹具中复核，未操作用户的应用窗口。完整 P0 不以本轮局部检查替代。

## Standards

1. `ResizableWorkbench` 的 blur / visibilitychange 原来只更新 React state；同一批事件的 pointerup 仍能读到旧 ref，取消后保存 310px。现所有取消入口同步清除 ref 与 state，完成入口同时检查活动视图、面板与作用域。原反例复测不再写入，正常拖动与键盘仍各保存一次。

该问题已关闭，无额外高价值判断项。独立 3 条 Rust 布局测试、5 条布局模型/错误提示用例通过；宽度 900→780px 后，源行与行内 208px 保持，scrollTop 随重排 1400→1686。

## Spec

1. PRD 要求 UI 持久化失败可见。早先的宽度保存失败若后面还有成功写入，曾不显示错误。现所有失败均记录到对应仓库，后继排队成功不抹去提示；独立反例转绿，模型 5/5。
2. PRD 5.3 / 5.4 要求键盘焦点可见。1024px 上下文抽屉打开后，Shift+Tab 曾到达被覆盖的“标记整个文件”。现焦点离开抽屉时收起，不重定向目标；进入设置弹层时保留抽屉，关闭设置后回到原按钮。两条路径均复测通过，未写布局。

两项均已关闭。字号 26px、25 行上下文、软换行长行下，连续拖动、两栏开合和 1440→1040→740→1440 均保持同一源行及 80px 行内偏移；缩窗不新增保存，Patch / Review 原对象不变。分隔条方向键、Shift、Home/End、Enter/Esc、直接输入、加减与范围恢复均通过定向检查。这些追加组件脚本属于独立临时验收，不声称已经全部纳入项目 CI。

## 主工作区与原生证据

完整 Rust 116 项通过，1 条真实 CLI opt-in 跳过；前端 17 项、类型检查、生产构建、三 crate Clippy、Rust 与前端格式检查通过。历史桥接启动计时波动本次未复现，原因仍未定位，不以单次全绿认定 NFR 达标。

独立原生包已完成：真实仓库保存宽度、Context 覆盖、linked worktree 继承、clone 隔离、保存失败提示/回滚/重试、进程正常退出后的重启读取、从 worktree 恢复默认。SQLite、index、源码和 Git 配置核对一致；详见 `ACCEPTANCE.md`。

本轮 Standards 发现 1 项、Spec 发现 2 项，全部修复并独立复验；没有剩余的本轮阻断项。
