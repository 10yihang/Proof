# Context 人工关联修正评审

范围：CTX-04，文件级 Context 阅读、原始会话的按需加载、同 Worktree 候选、备注/解除/恢复/撤销、修正历史和保留期。完整 PRD 的其他 P0 继续保留；此记录不表示整份产品已经交付。

固定基线为 `.artifacts/context-review-base/`：在上一轮诊断实现之后、CTX 代码之前保存的文件副本。新文件与基线的增量保存在 `.artifacts/context-review.patch`，另纳入 `real-git.spec.ts` 的实际 SQLite UI 验证、`local_data.rs` 的全局清理计数器修复及诊断 action/error 白名单。源码仓库 HEAD 为 `52aaf2a`；因为其后诊断与本轮改动尚未提交，不将整个 `git diff HEAD` 混称为单独 CTX 增量。

## Standards

独立审查报告两项硬问题：

1. P2：候选列表按最后活动排序并使用 OFFSET；未读第 31 个会话收到新事件后升到榜首，第二页漏掉该会话。改为稳定会话登记时间与 ID 的 keyset cursor。原反例及“新活动 + 已读前页记录清理”组合测试均通过。
2. P2：事件数量更新会自动重读第一页，移除用户已经加载的旧页。改为保留已有页、提示新记录。旧页正文、命令和展开状态在后台新增后保留，无额外重读。

最终独立定向复核：硬问题 0，判断项 0。隔离副本 2 条核心、2 条页面反例转绿；未修改主树或操作原生窗口。

## Spec

独立审查与后续复核报告三项问题：

1. 到期输出仍在已展开的缓存中显示，事件数量不变时不会更新。增加逐事件保留期限；两页输出与整条事件分别到期时清理，其他记录和展开状态保留。独立组件反例通过。
2. P2：同一持续会话的修正和历史到期后，最初的无修正版本再次有效。让到期但未删除的历史继续参与版本，并用 SQLite 删除触发器维护独立历史代次。原反例改为返回 `CONTEXT_CHANGED`；旧 helper 使用删除 SQL 同样触发失效。
3. P2：全局清理遗漏保留 `context_history_epoch`，同一进程重新打开仓库、授权及采集后 Context 报存储错误。将该无内容计数器纳入清理保留项；仅同步此文件后，独立原反例通过，不需要重启。

最终独立复核：已报告问题全部关闭；没有发现新增范围扩张。这不代表完整 P0 验收。

本轮 Standards 2 项、Spec 3 项发现均已修复。完整回归、应用包和原生交互结果另记入 `ACCEPTANCE.md`。
