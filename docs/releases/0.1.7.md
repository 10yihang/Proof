# Proof 0.1.7

- **分支页按路径前缀分组**：本地与远程分支按 `/` 前缀收进可折叠目录（如 `codex/backup/*`、`codex/xhst-stack/*`),数十个分支不再平铺。分组标题显示分支数量,点击折叠/展开;未分组分支保持原有展示。
- 分组内分支只显示最后一段名称,保留切换、当前分支标记与完整右键操作。
- 归档品牌图标备选方案与 0.1.2–0.1.6 期间的双轴评审记录(CODE-REVIEW-12—21)。

适用于 macOS Apple Silicon。0.1.1–0.1.6 可通过「设置 → 软件更新」升级;0.1.0 需手动下载安装包。

沿用现有 updater 签名密钥。macOS 包使用 ad-hoc 签名,尚未进行 Apple Developer ID 签名与公证。
